迭代器:
    iter() 
    next()

    iter: 创建迭代器对象
    next: 输出迭代器中的元素并自动迭代

    如果一个class需要支持迭代功能,需要实现迭代器和构造器的方法:
        def __iter__(self):
        def __next__(self):



生成器(yield):
    生成器是一个返回迭代器的函数,只能用于迭代操作,更简单点理解生成器就是一个迭代器

    在调用生成器运行的过程中,每次遇到 yield 时函数会暂停并保存当前所有的运行信息,返回 yield 的值,并在下一次执行 next() 方法时从当前位置继续运行


    